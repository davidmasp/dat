# {{ name }}

* Date: {{ today }}
* Author: {{ author }}

## Usage

Download or link the raw data using:

```
make download
```

Create the index and run the pipeline with:

```
make all
```

Delete data using:

```
make clean
```


