# data_pipeline_name

* Date: YYYY-MM-DD
* Author: YOUR_NAME

## Usage

Download or link the raw data using:

```bash
just download
```

Create the index and run the pipeline with:

```bash
just all
```

Delete data using:

```bash
just clean
```
