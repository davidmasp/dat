# Manuscript Folder

Minimal starter folder for a manuscript.

Keep manuscript text, notes, and local pulled assets together here.
The `artifacts/` subfolder is the manuscript-local equivalent of the
current global `figures/` workflow: configure source files in
`artifacts/input.toml`, pull them locally, and verify them with checksums.
